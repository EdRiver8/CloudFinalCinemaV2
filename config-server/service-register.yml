server:
  port: 9010

eureka:
  instace:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instace.hostname}:${server.port}/eureka/
